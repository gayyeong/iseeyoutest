import 'package:flutter/material.dart';

class RecordDetail2 extends StatelessWidget {
  const RecordDetail2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: BackButton());
  }
}
