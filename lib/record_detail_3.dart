import 'package:flutter/material.dart';

class RecordDetail3 extends StatelessWidget {
  const RecordDetail3({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: BackButton());
  }
}
